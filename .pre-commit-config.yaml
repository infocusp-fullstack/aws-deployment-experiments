repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.8 # Use latest Ruff version
    hooks:
      - id: ruff
        args: [--fix, --select, I] # Fix and sort imports
      - id: ruff-format
